# ShiftStart device-optimisation patch

Extract this folder into the repository root and run:

```bash
python3 shiftstart-device-optimisation-patch/apply-device-optimisation.py
```

The patch changes only layouts, interface JavaScript, Tailwind source, validation and documentation. It verifies that all 421 procedures and 446 symptoms remain present.
